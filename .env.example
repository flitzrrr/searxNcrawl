SEARXNG_URL=http://searxng:8080
SEARXNG_USERNAME=
SEARXNG_PASSWORD=

# Authentication for crawling protected pages (all optional)
CRAWL_AUTH_STORAGE_STATE=       # Path to Playwright storage state JSON
CRAWL_AUTH_COOKIES_FILE=        # Path to cookies JSON file
CRAWL_AUTH_PROFILE=             # Path to persistent browser profile directory
